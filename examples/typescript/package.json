{
  "dependencies": {
    "jest": "^26.6.1",
    "jest-pa11y": "^1.1.5"
  },
  "description": "End-to-end test for `jest-pa11y` + TypeScript",
  "devDependencies": {
    "ts-jest": "^26.4.3",
    "typescript": "^4.0.5"
  },
  "name": "jest-pa11y-e2e-ts",
  "private": true,
  "scripts": {
    "test": "JEST_PA11Y_SERVER_PORT=4002 jest --config .jestrc.js",
    "tsc": "tsc"
  }
}
